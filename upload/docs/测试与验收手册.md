# 测试与验收手册（执行报告版）

版本：v3.0  
更新时间：2026-04-23  
报告周期：2026-04-23（单次执行）  
测试范围：需求分析 v3.0 覆盖的全模块能力

---

## 1. 报告概览

### 1.1 本次结论摘要

1. 后端构建通过。  
2. 后端自动化测试通过（9 套件、27 用例）。  
3. LLM 相关最小测试通过（3 套件、7 用例）。  
4. AI 脚本帮助命令可执行。  
5. 果实分级数据集质量检查无 error，但存在 3 条结构偏置 warning。  
6. 端到端小程序真机人工回归本次未执行（需联调环境补测）。

### 1.2 测试方法

1. 自动化：后端 Jest + 脚本可执行性检查。  
2. 静态契约核对：Controller/Service/DTO 与文档一致性。  
3. 数据质量检查：AI 数据集脚本扫描。  
4. 手工场景：本报告给出步骤与预期，标记待执行项。

---

## 2. 测试环境

### 2.1 运行环境

1. OS：Windows（本地工作区）。  
2. Node.js：`v24.12.0`。  
3. npm：`11.6.2`。  
4. Python：`3.13.2`。

### 2.2 代码范围

1. 后端：`backend/src/modules/*`、`backend/src/llm/*`。  
2. 小程序：`pages/*`、`services/*`。  
3. AI：`ai/service/*`、`ai/scripts/*`。

说明：本工作区为进行中开发分支状态，测试基于当前目录实时代码，不绑定单独发布 tag。

---

## 3. 自动化与命令执行结果

## 3.1 后端构建与测试

### 用例 A-001：后端构建

命令：

```bash
cd backend
npm run build
```

结果：通过。  
关键输出：`nest build` 正常结束。

### 用例 A-002：后端全量单测

命令：

```bash
cd backend
npm test -- --runInBand
```

结果：通过。  
关键输出：

1. `Test Suites: 9 passed, 9 total`  
2. `Tests: 27 passed, 27 total`

日志备注：运行期间出现若干 `WARN`（模拟依赖失败与回退分支），符合预期测试覆盖方向，不影响通过结论。

### 用例 A-003：LLM 最小测试

命令：

```bash
cd backend
npm test -- llm.utils.spec.ts llm.service.spec.ts ai-narrative.service.spec.ts --runInBand
```

结果：通过。  
关键输出：

1. `Test Suites: 3 passed, 3 total`  
2. `Tests: 7 passed, 7 total`

---

## 3.2 AI 脚本可执行性

### 用例 A-004：病害训练脚本帮助

命令：

```bash
cd ai
python scripts/train_disease.py --help
```

结果：通过。  
结论：训练脚本入口可用，参数解析正常。

### 用例 A-005：病害推理脚本帮助

命令：

```bash
cd ai
python scripts/predict_disease.py --help
```

结果：通过。  
结论：推理脚本入口可用，参数解析正常。

### 用例 A-006：果实分级脚本帮助

命令：

```bash
cd ai
python scripts/predict_fruit_grade.py --help
```

结果：通过。

### 用例 A-007：风险评估脚本帮助

命令：

```bash
cd ai
python scripts/risk_assess.py --help
```

结果：通过。

---

## 3.3 数据集质量检查

### 用例 A-008：严格模式数据质量检查

命令：

```bash
cd ai
python scripts/check_fruit_grade_dataset.py --dataset-root datasets/fruit_grade/v1 --strict --json
```

结果：未通过（预期内）。  
输出结论：`errorCount=0, warningCount=3`，严格模式将 warning 视为失败返回码。

warning 明细：

1. `features.region` 头部占比 1.000。  
2. `features.channel` 头部占比 1.000。  
3. `features.package_type` 头部占比 1.000。

### 用例 A-009：普通模式数据质量检查

命令：

```bash
cd ai
python scripts/check_fruit_grade_dataset.py --dataset-root datasets/fruit_grade/v1 --json
```

结果：通过。  
输出结论：无 error，存在上述 3 条 warning。

---

## 4. 接口契约回归结果（稳定字段）

## 4.1 病害识别稳定字段

接口：`POST /ai/disease/predict`

检查结果：通过（代码级核对）。  
字段确认：

1. `label`  
2. `confidence`  
3. `severity`  
4. `advice`  
5. `needManualReview`

## 4.2 果实分级稳定字段

接口：`POST /ai/fruit/grade`

检查结果：通过（代码级核对）。  
字段确认：

1. `variety`  
2. `grade`  
3. `colorScore`  
4. `defectRatio`  
5. `sizeScore`  
6. `maturityScore`  
7. `retailMinPrice/retailMaxPrice`  
8. `wholesaleMinPrice/wholesaleMaxPrice`  
9. `reason`  
10. `riskWarning`  
11. `factors`

## 4.3 风险评估稳定字段

接口：`GET /api/risk/:batchId`

检查结果：通过（代码级核对）。  
字段确认：`level/levelText/reason/suggestion`。

## 4.4 统一响应结构

检查结果：通过。  
依据：全局 `TransformInterceptor` + `HttpExceptionFilter`。

---

## 5. 功能测试用例矩阵（步骤级）

说明：以下用例按“每个功能”给出标准执行步骤。状态分为：

1. `通过`：本次已自动化或已执行。  
2. `待执行`：需联调环境手工验证。

## 5.1 认证与用户

### TC-AUTH-01 登录成功

状态：待执行（手工）。

步骤：

1. 打开小程序 `login` 页。  
2. 输入有效手机号与验证码。  
3. 点击登录。  
4. 观察接口 `/api/auth/login` 返回。  
5. 观察后续 `/api/auth/profile` 返回。

预期：

1. 返回 token。  
2. 用户信息展示正常。  
3. 页面跳转到 `mine`。

### TC-AUTH-02 未登录访问受保护接口

状态：待执行（手工）。

步骤：

1. 清空本地 token。  
2. 访问任意受保护页面触发请求。  
3. 观察请求拦截行为。

预期：

1. 前端提示“Please login first”。  
2. 自动跳转 `mine`。

### TC-AUTH-03 退出登录

状态：待执行（手工）。

步骤：

1. 在 `mine` 点击退出。  
2. 确认弹窗。  
3. 检查本地 token 与 userInfo。

预期：

1. 后端返回 success。  
2. 本地会话清理完成。

## 5.2 批次管理

### TC-BATCH-01 批次创建

状态：待执行（手工）。

步骤：

1. 进入 `batch-create`。  
2. 填写名称、品种、地块、面积、日期。  
3. 提交。  
4. 查看批次列表与产品列表。

预期：

1. 批次创建成功。  
2. 自动生成默认产品。

### TC-BATCH-02 批次详情统计

状态：待执行（手工）。

步骤：

1. 进入批次详情。  
2. 对比日志数量与产品数量。

预期：`logsCount/productsCount` 与实际一致。

### TC-BATCH-03 更新阶段

状态：待执行（手工）。

步骤：

1. 在批次详情打开阶段面板。  
2. 选择新阶段并提交。  
3. 重新刷新详情。

预期：阶段更新成功并持久化。

## 5.3 农事日志

### TC-LOG-01 创建日志（含图片）

状态：待执行（手工）。

步骤：

1. 进入日志创建页。  
2. 选择批次、类型、内容。  
3. 上传图片。  
4. 提交。  
5. 返回列表查看。

预期：日志创建成功，图片 URL 可访问。

### TC-LOG-02 兼容字段创建

状态：待执行（接口）。

步骤：

1. 用 `batch_id/log_type/description/image_url` 调 `/api/logs`。  
2. 查询列表确认。

预期：兼容字段正确写入。

### TC-LOG-03 日志类型字典

状态：待执行（接口）。

步骤：

1. 调 `GET /api/logs/types`。  
2. 检查类型枚举。

预期：返回固定类型数组。

## 5.4 病害识别

### TC-DIS-01 主路径识别

状态：待执行（手工）。

步骤：

1. `disease-recognize` 上传图片。  
2. 调 `/ai/disease/predict`。  
3. 保存为日志。

预期：

1. 返回 label/confidence/severity/advice。  
2. 可保存日志成功。

### TC-DIS-02 远程 AI 失败回退

状态：待执行（环境控制）。

步骤：

1. 关闭/错误配置 `DISEASE_AI_BASE_URL`。  
2. 调预测接口。  
3. 查看响应与日志。

预期：返回本地规则结果，不报 500。

### TC-DIS-03 记录分页查询

状态：待执行（接口）。

步骤：

1. 调 `/api/disease/records?page=1&pageSize=10`。  
2. 检查 total 与 list。

预期：分页结构正确。

## 5.5 风险预警

### TC-RISK-01 风险评估接口

状态：待执行（接口/手工）。

步骤：

1. 调 `/api/risk/assessment?batchId=...`。  
2. 检查 `level/levelText/reason/suggestion`。  
3. 调 `/api/risk/:batchId` 对比。

预期：两个入口结果结构一致。

### TC-RISK-02 历史兼容参数

状态：待执行（接口）。

步骤：

1. 分别用 `batchId` 和 `batch_id` 调历史接口。  
2. 比较结果。

预期：结果一致。

### TC-RISK-03 无历史自动补评估

状态：待执行（数据准备后）。

步骤：

1. 清空某用户风险记录。  
2. 调 `/api/risk/summary`。  
3. 再查 risk_records。

预期：自动生成并返回摘要统计。

## 5.6 果实分级与定价

### TC-PRICE-01 图片分级

状态：待执行（手工）。

步骤：

1. 在 `fruit-grade` 上传图。  
2. 选择渠道与包装。  
3. 提交。  
4. 查看分数和价格区间。

预期：返回完整字段，页面渲染成功。

### TC-PRICE-02 无图结构化分级

状态：待执行（手工/接口）。

步骤：

1. 不上传图，填写直径/糖度/重量/缺陷等级。  
2. 提交。

预期：仍可输出等级和价格。

### TC-PRICE-03 感知回退

状态：待执行（环境控制）。

步骤：

1. 关闭 AI 感知服务。  
2. 调 `/ai/fruit/grade`。  
3. 查看 `source.engine`。

预期：落入本地回退，主流程不中断。

### TC-PRICE-04 规则引擎单测

状态：通过（自动化）。

依据：`price-engine.spec.ts`、`price.service.spec.ts` 通过。

## 5.7 产品与二维码

### TC-PROD-01 产品列表

状态：待执行（接口）。

步骤：

1. 调 `/api/products`。  
2. 检查分页与筛选。

预期：返回产品列表与批次基础信息。

### TC-PROD-02 生成二维码（优先小程序码）

状态：待执行（手工）。

步骤：

1. 调 `/api/products/:id/qrcode`。  
2. 检查 `qrcodeType/qrcodeFallback`。  
3. 小程序端预览并保存。

预期：成功返回 base64，失败时自动回退普通二维码。

## 5.8 溯源与验真

### TC-TRACE-01 查询溯源详情

状态：待执行（接口）。

步骤：

1. 调 `/api/trace/:code`。  
2. 核对 batch/log/disease/proof 信息。

预期：返回聚合数据完整。

### TC-TRACE-02 验真接口

状态：待执行（接口）。

步骤：

1. 调 `/api/trace/:code/verify`。  
2. 查看 `verified/status/message/proofHash`。

预期：返回哈希比对结果。

### TC-TRACE-03 provider 回退

状态：通过（自动化覆盖分支）。

依据：

1. `hash-proof.provider.spec.ts` 通过。  
2. `notary-anchor.provider.spec.ts` 通过。  
3. `evm-anchor.provider.spec.ts` 通过。  
4. `trace.service.spec.ts` 通过。

## 5.9 天气与上传

### TC-WEA-01 当前天气

状态：待执行（接口）。

步骤：

1. 调 `/api/weather/current`。  
2. 同参数重复调用。

预期：第二次命中缓存。

### TC-WEA-02 15 天预报

状态：待执行（接口）。

步骤：

1. 调 `/api/weather/forecast?days=15`。  
2. 检查 days 长度。

预期：返回最多 15 天。

### TC-UP-01 图片上传

状态：待执行（接口）。

步骤：

1. 上传 jpg/png/webp。  
2. 上传超大文件与非法 MIME。

预期：合法文件成功，非法文件返回业务错误。

---

## 6. LLM 与 AI 边界验证

### 6.1 边界检查结果

状态：通过（代码核对 + 单测）。

结论：

1. 风险等级由规则计算，LLM 仅润色文本。  
2. 分级价格由规则/模型计算，LLM 不改数值。  
3. 病害识别结论由本地/远程模型输出，LLM 仅补建议。

### 6.2 LLM 失败回退验证

状态：通过（单测覆盖 + 代码核对）。

结论：

1. `llm.service` 提供 fallback 文案。  
2. 解析失败、HTTP 失败、配置缺失均可回退。

---

## 7. 缺陷与风险清单

## 7.1 缺陷

1. 本轮自动化测试未发现阻断缺陷。

## 7.2 已知风险

1. 数据集结构偏置（region/channel/package_type）可能影响泛化。  
2. 前端 mock fallback 若在生产打开，会掩盖真实后端问题。  
3. `logout` 仍是 MVP 语义，缺乏服务端会话失效治理。

---

## 8. 验收结论与建议

### 8.1 当前验收判定

1. 自动化质量：通过。  
2. 契约稳定性：通过（代码核对）。  
3. 端到端业务验收：待执行（需联调环境手工回归）。

### 8.2 上线前必须补测项

1. 小程序真机全流程（登录、批次、日志、病害、风险、分级、二维码、溯源）。  
2. 关键回退场景（AI down、LLM down、天气异常、锚定失败）。  
3. 真机扫码打开 `pages/trace-view/index` 的体验验收。

### 8.3 推荐后续动作

1. 将“稳定字段检查”纳入自动化契约测试。  
2. 在 CI 增加 `check_fruit_grade_dataset --strict` 作为数据门禁。  
3. 建立回退率与异常率周报。
